import{NextResponse}from'next/server';import{publicOpportunity,viewOpportunity}from'../../../lib/server-company';export async function GET(_:Request,{params}:{params:Promise<{opportunityId:string}>}){const{opportunityId}=await params;const existing=await publicOpportunity(opportunityId);if(!existing)return NextResponse.json({error:'Opportunity not found'},{status:404});const opportunity=await viewOpportunity(opportunityId);return NextResponse.json({opportunity});}
