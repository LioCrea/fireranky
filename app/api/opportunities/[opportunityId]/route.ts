import{NextResponse}from'next/server';import{publicOpportunity}from'../../../lib/server-company';export async function GET(_:Request,{params}:{params:Promise<{opportunityId:string}>}){const{opportunityId}=await params;const opportunity=await publicOpportunity(opportunityId);return opportunity?NextResponse.json({opportunity}):NextResponse.json({error:'Opportunity not found'},{status:404});}
