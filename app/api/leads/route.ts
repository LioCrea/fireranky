import{NextResponse}from'next/server';import{listLeads,registerLead}from'../../lib/server-agreement';
export async function GET(req:Request){try{const leads=await listLeads(req);console.log('[FireRanky LEADS] loaded',leads.length);return NextResponse.json({leads})}catch(e){console.error('[FireRanky LEADS] GET error',e);return NextResponse.json({error:e instanceof Error?e.message:'Unable to load leads'},{status:400})}}
export async function POST(req:Request){try{const body=await req.json();const lead=await registerLead(req,body.agreementId,body);console.log('[FireRanky LEADS] registered',lead.leadId);return NextResponse.json({lead},{status:201})}catch(e){console.error('[FireRanky LEADS] POST error',e);return NextResponse.json({error:e instanceof Error?e.message:'Unable to register lead'},{status:400})}}
