import{NextResponse}from'next/server';import{companyFires,companyIdentity,companyOpportunities,seedVantaGrid}from'../../../lib/server-company';
export async function GET(req:Request){try{const{identity}=await companyIdentity(req);let ops=await companyOpportunities(identity.userId);if(!ops.length){await seedVantaGrid(req);ops=await companyOpportunities(identity.userId)}const fires=await companyFires(identity.userId,ops.map((o:any)=>o.opportunityId));return NextResponse.json({opportunities:ops,fires});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to load dashboard'},{status:400})}}
