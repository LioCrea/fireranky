import{NextResponse}from'next/server';import{DynamoDBClient}from'@aws-sdk/client-dynamodb';import{DynamoDBDocumentClient,PutCommand}from'@aws-sdk/lib-dynamodb';import{demoOpportunities}from'../../../lib/demo-opportunities';const region=process.env.AWS_REGION||'eu-west-3',table=process.env.FIRERANKY_OPPORTUNITIES_TABLE||'fireranky-opportunities-dev',ddb=DynamoDBDocumentClient.from(new DynamoDBClient({region}),{marshallOptions:{removeUndefinedValues:true}});export async function POST(){if(process.env.NODE_ENV==='production')return NextResponse.json({error:'Seeder disabled in production'},{status:403});for(const item of demoOpportunities)await ddb.send(new PutCommand({TableName:table,Item:item}));return NextResponse.json({seeded:demoOpportunities.length});}
