import{DynamoDBClient}from'@aws-sdk/client-dynamodb';import{DynamoDBDocumentClient,GetCommand,PutCommand,ScanCommand,UpdateCommand}from'@aws-sdk/lib-dynamodb';import{identityFromRequest,getProfile}from'./server-profile';import{publicOpportunity}from'./server-company';const region=process.env.AWS_REGION||'eu-west-3',agreements=process.env.FIRERANKY_AGREEMENTS_TABLE||'fireranky-agreements-dev',leads=process.env.FIRERANKY_LEADS_TABLE||'fireranky-leads-dev',fires=process.env.FIRERANKY_FIRES_TABLE||'fireranky-fires-dev';const ddb=DynamoDBDocumentClient.from(new DynamoDBClient({region}),{marshallOptions:{removeUndefinedValues:true}});export async function createAgreement(req:Request,fireId:string){const identity=await identityFromRequest(req);if(identity.role!=='company')throw new Error('Company account required.');const fr=await ddb.send(new GetCommand({TableName:fires,Key:{fireId}}));const fire:any=fr.Item;if(!fire||fire.status!=='CALL_ACCEPTED')throw new Error('Rep must accept the call first.');const op:any=await publicOpportunity(fire.opportunityId);if(!op||op.companyId!==identity.userId)throw new Error('Opportunity not owned by this company.');const company:any=await getProfile(identity.userId),now=new Date().toISOString(),agreementId=fireId;const item={agreementId,fireId,opportunityId:fire.opportunityId,companyId:identity.userId,repId:fire.repId,status:'AWAITING_REP',version:1,terms:{companyName:company?.companyName||op.companyName,repName:`${fire.repSnapshot?.firstName||''} ${fire.repSnapshot?.lastName||''}`.trim(),campaign:op.title,territory:op.territory,commission:op.commission,basis:op.basis,paymentDays:30,leadProtectionDays:90,tailDays:60,leadChallengeHours:48},companyAcceptedAt:now,createdAt:now,updatedAt:now};await ddb.send(new PutCommand({TableName:agreements,Item:item}));await ddb.send(new UpdateCommand({TableName:fires,Key:{fireId},UpdateExpression:'SET #s=:s, agreementId=:a, updatedAt=:u',ExpressionAttributeNames:{'#s':'status'},ExpressionAttributeValues:{':s':'AGREEMENT_PENDING',':a':agreementId,':u':now}}));return item;}export async function getAgreementFor(req:Request,agreementId:string){const identity=await identityFromRequest(req),r=await ddb.send(new GetCommand({TableName:agreements,Key:{agreementId}})),a:any=r.Item;if(!a||![a.repId,a.companyId].includes(identity.userId))throw new Error('Agreement not found.');return a;}export async function acceptAgreement(req:Request,agreementId:string){const identity=await identityFromRequest(req),a:any=await getAgreementFor(req,agreementId);if(identity.userId!==a.repId)throw new Error('Sales rep required.');const now=new Date().toISOString();const r=await ddb.send(new UpdateCommand({TableName:agreements,Key:{agreementId},UpdateExpression:'SET #s=:s, repAcceptedAt=:u, activatedAt=:u, updatedAt=:u',ExpressionAttributeNames:{'#s':'status'},ExpressionAttributeValues:{':s':'ACTIVE',':u':now},ReturnValues:'ALL_NEW'}));await ddb.send(new UpdateCommand({TableName:fires,Key:{a.fireId},UpdateExpression:'SET #s=:s, updatedAt=:u',ExpressionAttributeNames:{'#s':'status'},ExpressionAttributeValues:{':s':'ACTIVE_REP',':u':now}}));return r.Attributes;}export async function listAgreements(req:Request){const identity=await identityFromRequest(req),r=await ddb.send(new ScanCommand({TableName:agreements}));return(r.Items||[]).filter((a:any)=>a.repId===identity.userId||a.companyId===identity.userId);}export async function registerLead(req:Request,agreementId:string,body:any){const identity=await identityFromRequest(req),a:any=await getAgreementFor(req,agreementId);if(identity.userId!==a.repId||a.status!=='ACTIVE')throw new Error('Active rep agreement required.');const name=String(body.companyName||'').trim();if(!name)throw new Error('Prospect company is required.');const now=new Date().toISOString(),leadId=`${agreementId}#${Date.now().toString(36)}`;const item={leadId,agreementId,opportunityId:a.opportunityId,companyId:a.companyId,repId:a.repId,prospectCompany:name,contactName:String(body.contactName||''),contactEmail:String(body.contactEmail||''),status:'REGISTERED',registeredAt:now,challengeDeadline:new Date(Date.now()+a.terms.leadChallengeHours*3600000).toISOString(),protectionUntil:new Date(Date.now()+a.terms.leadProtectionDays*86400000).toISOString(),createdAt:now,updatedAt:now};await ddb.send(new PutCommand({TableName:leads,Item:item}));return item;}export async function listLeads(req:Request){const identity=await identityFromRequest(req),r=await ddb.send(new ScanCommand({TableName:leads}));return(r.Items||[]).filter((l:any)=>l.repId===identity.userId||l.companyId===identity.userId).sort((a:any,b:any)=>b.createdAt.localeCompare(a.createdAt));}
